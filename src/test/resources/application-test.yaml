spring:
  datasource:
    url: jdbc:h2:mem:store;DB_CLOSE_DELAY=-1;MODE=MySQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false
    baselineOnMigrate: false
  jwt:
    secret: test-secret-key-should-be-32-bytes-minimum-123456
    accessTokenExpiration: 900
    refreshTokenExpiration: 604800
stripe:
  secretKey: test
  webhookSecretKey: test
websiteUrl: http://localhost:8080
